;-----STRING CONCAT-----
	
	LEA DI,STR3+2

	LEA SI,STR1+2

	L4:
	CMP BYTE PTR[SI],"$"
	JE OUTL4
	MOV DL,[SI]
	MOV [DI],DL
	INC DI
	INC SI
	JNE L4
	OUTL4:

	LEA SI,STR2+2

	DEC DI				

	L5:
	CMP BYTE PTR[SI],"$"
	JE OUTL5
	MOV DL,[SI]
	MOV [DI],DL
	INC DI
	INC SI
	JNE L5
	OUTL5:

	INC DI
	MOV DL,"$"
	MOV BYTE PTR [DI], DL

	MOV AH,02H
	MOV DL,0AH
	INT 21H

	MOV AH,09H
	LEA DX,STR3+2
	INT 21H
